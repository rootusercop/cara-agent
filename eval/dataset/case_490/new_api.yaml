openapi: "3.0.0"
info: {title: API, version: "1.0"}
paths:
  /api/promotions:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  country: {type: string}
                  type: {type: string}
                  expires_at: {type: string}
