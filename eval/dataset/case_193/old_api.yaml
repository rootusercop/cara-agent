openapi: "3.0.0"
info:
  title: Service API
  version: "1.0"
paths:
  /api/notifications:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  state: {type: string}
