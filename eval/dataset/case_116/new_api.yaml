openapi: "3.0.0"
info:
  title: Service API
  version: "v2"
paths:
  /v2/audit_logs:
    get:
      summary: Endpoint
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  type: {type: string}
                  id: {type: string}
