openapi: "3.0.0"
info:
  title: Service API
  version: "1.0"
paths:
  /api/coupons:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  is_verified: {type: string}
                  is_active: {type: string}
                  account_id: {type: string}
