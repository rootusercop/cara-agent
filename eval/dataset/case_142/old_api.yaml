openapi: "3.0.0"
info:
  title: Service API
  version: "1.0"
paths:
  /api/promotions:
    post:
      summary: Create
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, name]
              properties:
                name: {type: string}
                name: {type: string}
      responses:
        "201":
          description: Created
