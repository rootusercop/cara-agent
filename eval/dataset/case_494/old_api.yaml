openapi: "3.0.0"
info: {title: API, version: "1.0"}
paths:
  /api/analytics:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  quantity: {type: string}
                  account_id: {type: string}
                  address: {type: string}
