openapi: "3.0.0"
info: {title: API, version: "1.0"}
paths:
  /api/analytics:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  description: {type: string}
                  expires_at: {type: string}
                  phone: {type: string}
