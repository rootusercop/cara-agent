openapi: "3.0.0"
info:
  title: Service API
  version: "1.0"
paths:
  /api/coupons:
    get:
      summary: Get
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  email: {type: integer}
