openapi: "3.0.0"
info: {title: API, version: "1.0"}
paths:
  /api/accounts:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  user_id: {type: string}
                  score: {type: string}
                  country: {type: string}
