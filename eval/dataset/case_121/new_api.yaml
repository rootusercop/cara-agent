openapi: "3.0.0"
info:
  title: Service API
  version: "1.0"
paths:
  /api/memberships:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  type: {type: string}
                  notes: {type: string}
                  is_verified: {type: string}
