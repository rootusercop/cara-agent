openapi: "3.0.0"
info:
  title: Service API
  version: "1.0"
paths:
  /api/analytics:
    get:
      summary: Endpoint
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  quantity: {type: string}
                  tier: {type: string}
