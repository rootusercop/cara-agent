openapi: "3.0.0"
info:
  title: Service API
  version: "1.0"
paths:
  /api/devices:
    get:
      summary: Endpoint
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_at: {type: string}
                  is_verified: {type: string}
