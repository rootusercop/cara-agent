openapi: "3.0.0"
info: {title: API, version: "1.0"}
paths:
  /api/suppliers:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  notes: {type: string}
                  country: {type: string}
                  description: {type: string}
