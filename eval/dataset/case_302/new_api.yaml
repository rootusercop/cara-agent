openapi: "3.0.0"
info: {title: API, version: "1.0"}
paths:
  /api/payments:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  metadata: {type: integer, format: int64}
