openapi: "3.0.0"
info:
  title: Service API
  version: "1.0"
paths:
  /api/claims:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  tier: {type: string}
                  category: {type: string}
                  account_id: {type: string}
