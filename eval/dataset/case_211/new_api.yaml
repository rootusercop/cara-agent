openapi: "3.0.0"
info:
  title: Service API
  version: "1.0"
paths:
  /api/policies:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  category: {type: string}
                  enabled: {type: string}
                  description: {type: string}
