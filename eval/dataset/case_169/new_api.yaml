openapi: "3.0.0"
info:
  title: Service API
  version: "1.0"
paths:
  /api/incidents:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
                properties:
                  score: {type: string}
                  deleted_at: {type: string}
                  rank: {type: string}
